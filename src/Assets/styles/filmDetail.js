import { boxShadow, themeColor } from './theme'

export const filmDetail = theme => ({
  card: {
    boxShadow: boxShadow,
    width: "100%"
  },
  title: {
    fontWeight: "bold",
    color: themeColor,
  },
});